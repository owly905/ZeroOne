if (!hasInterface) exitWith {}; private["_deadVehicle","_cargoIndex"]; _deadVehicle = param[0,objNull,[objNull]]; if(isNull _deadVehicle) exitWith {}; if(local _deadVehicle) then { _deadVehicle lock 0; } else { [[_deadVehicle,0],"zero_fnc_lockVehicle",_deadVehicle] call zero_fnc_sendPacket; }; player moveInDriver _deadVehicle; { if(!alive _x) then { _cargoIndex = _deadVehicle getCargoIndex _x; player assignAsCargoIndex [_deadVehicle,_cargoIndex]; uiSleep 0.01;
player moveInCargo _deadVehicle; uiSleep 0.5; [[player, "zero_var_fnc_setPos", [_x, (_deadVehicle modelToWorld [-2,_cargoIndex*-0.5,-1])]], "zero_fnc_remoteExecuteCommand", zero_var_HC1_owner] call zero_fnc_sendPacket; }; } forEach (crew _deadVehicle - [driver _deadVehicle]); player action ["Eject", vehicle player]; uiSleep 0.5; moveOut player; if(local _deadVehicle) then { _deadVehicle lock 2; } else { [[_deadVehicle,2],"zero_fnc_lockVehicle",_deadVehicle] call zero_fnc_sendPacket; };
